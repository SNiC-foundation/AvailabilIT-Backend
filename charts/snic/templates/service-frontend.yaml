apiVersion: v1
kind: Service
metadata:
  name: {{ include "snic.fullname" . }}-frontend
  labels:
    {{- include "snic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snic.selectorLabels.frontend" . | nindent 4 }}
